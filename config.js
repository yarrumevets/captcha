const config = {
  SERVER_PORT: 4191,
  TIMEOUT: 30_000, // 30 seconds.
  NOISE: 3.5, // At 4.0 it is very difficult to read.
  TEXT_LENGTH: 4, // 4 to 8
  BACKGROUND_COLOR: "#EEE",
  TEXT_COLOR: "#000",
};

export default config;
